// Çıplak TS paketi — Next/Expo'yu tanımaz, ortak paket tabanını kullanır (bkz.
// @repo/config/eslint/package.mjs, packages/types/eslint.config.mjs örneği).
import packageConfig from '@repo/config/eslint/package.mjs'

export default packageConfig
