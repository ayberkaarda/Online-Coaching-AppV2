{
  // Paylaşılan katı derleyici tabanı @repo/config'ten gelir (ADR-0023 madde 1, Faz 4.5
  // commit 3). `include`/`exclude` app'e özgü olduğu için burada kalır.
  "extends": "@repo/config/tsconfig/e2e.json",
  "include": ["tests/e2e/**/*.ts", "playwright.config.ts"],
  "exclude": ["node_modules"]
}
